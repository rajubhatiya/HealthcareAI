apiVersion: v1
kind: Service
metadata:
  name: healthcare-agent-service
spec:
  type: LoadBalancer
  selector:
    app: healthcare-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
